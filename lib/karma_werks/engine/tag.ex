defmodule KarmaWerks.Engine.Tag do
  @moduledoc """
  Tag related functions are here
  """
  def get_tag_from_uid(uid) do
    :implement_me
  end

  def add_tag(uid, name) do
    :implement_me
  end

  def remove_tag(uid, name) do
    :implement_me
  end

  def get_tagged_nodes(uid) do
    :implement_me
  end
end
